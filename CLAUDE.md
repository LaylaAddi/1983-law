# CLAUDE.md — Standing Instructions

After every session where code changes are made:
1. Always end with the git commands to:
   - Fetch/pull the dev branch locally
   - Merge to master
   - Push master to trigger Render deployment
2. Always update HANDOFF.md with what was done this session before signing off.
